# Normalize line endings across platforms. Keeps shell scripts LF so they run
# on Linux/macOS (a CRLF shebang breaks `env bash`) and makes the generated
# Cursor/Codex adapters build reproducibly regardless of contributor OS.
* text=auto eol=lf
